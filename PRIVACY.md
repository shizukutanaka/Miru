# プライバシーポリシー

最終更新: 2026-04-29

## 結論を先に

**Miru は利用者のデータを収集しません。** テレメトリなし、トラッキングなし、
クラッシュレポートも完全に opt-in。詳細を以下に記載します。

## 収集しないもの

- 利用統計 / アナリティクス
- IP アドレス
- 接続先・接続元の履歴
- 画面の内容
- 入力イベント (キーボード / マウス)
- 音声 / 動画データ
- ファイル名 / 内容
- メールアドレス / アカウント情報 (そもそもアカウント不要)

## 例外的に処理されるもの

### シグナルサーバー (オプション)

シグナルサーバーを使う場合(`MIRU_SIGNAL` で指定する、あなた自身がホストする
サーバーです — 公式にホストされたシグナルサーバーは現時点で存在しません)、
以下が**一時的に**処理されます:

- デバイス ID (XXXX-XXXX 形式の8桁) — 接続先発見のため
- 公開 IP アドレス — NAT 越えのため
- ピア間の接続要求 — 関係マッチングのため

**保存しません。** メモリ上のみで、ピア接続成立または60秒タイムアウトで消去。
ログも残しません。完全セルフホスト可能で、その場合は私たちすら接触しません。

### リレーサーバー (P2P 失敗時のフォールバック)

直接 P2P が失敗した場合のみ、リレーサーバー経由で暗号文を中継します:

- リレーは **暗号文しか見えません** (E2E 暗号化済み)
- セッショントークンと接続元/先のみメモリ上で関連付け
- セッション終了で完全消去

### クラッシュレポート (未実装 — 計画中)

**この機能はまだ実装されていません**(`--enable-crash-reports` フラグはコード上
存在せず、送信先エンドポイントも確保されていません)。実装される場合は以下の
設計を予定していますが、現時点でクラッシュ情報が外部に送信されることはありません:

- opt-in(明示指定した場合のみ)
- スタックトレース / OS バージョン / Miru バージョンのみ
- 個人情報・パス・URL の自動除去(差分プライバシーノイズ + 正規表現フィルタ)
- 90日後自動削除

実装状況は `docs/roadmap.md` セクション0.5(フェーズ3)を参照してください。

## あなたのデバイスに保存されるもの

| 場所 | 内容 |
|------|------|
| `~/.config/miru/identity` | Ed25519 秘密鍵 (mode 600) |
| `~/.config/miru/acl.json` | 信頼済みデバイスのリスト |
| `~/.config/miru/agent-audit.log` | AI エージェント操作の audit log |
| `~/.config/miru/recordings/` | (録画 opt-in 時) セッション録画 |

すべて **あなたのマシンに留まります**。クラウド同期なし。
削除すると即座に痕跡が消えます。

## 法的根拠 (GDPR / CCPA)

私たちが処理するデータがほぼないため、適用される法的根拠は最小限です。
形式的には:
- **正当な利益** (GDPR Art. 6(1)(f)): シグナル/リレー機能の提供
- **同意** (Art. 6(1)(a)): クラッシュレポートのみ

EU/英国/カリフォルニア在住者の個人データに関する権利 (アクセス、削除、ポータビリティ等) は
適用されますが、対象データがほぼないため実務的には無関係です。

## 連絡

現時点で専用窓口は未設置です。GitHub Issues でご連絡ください
(`privacy@miru.app` はまだ稼働していません)。

## 履歴

- 2026-04-29: 初版
